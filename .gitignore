node_modules/
.env
cache/
artifacts/
coverage/
coverage.json
typechain/
typechain-types/
# build output / local deployment config (regenerated by deploy).
# config.js ships with a committed placeholder; deploy overwrites it locally.
public/deployment.json
npm-debug.log*
.DS_Store

# blackjack durable guest-bank state (runtime, never commit)
.bj-bank.json
.bj-bank*.json
server/.bj-bank.json
