// Default placeholder — overwritten by `npm run deploy:local` / `deploy:sepolia`.
// If `address` is null the UI shows a "deploy the contract first" notice.
window.COINFLIP_CONFIG = {
  address: null,
  chainId: 31337,
  network: "localhost",
  treasury: null,
  vrfCoordinator: null,
  abi: [],
};
